# NOTE: Some packages require special installation on Termux:
# - google-generativeai: requires grpcio (installed via pkg)
# - Pillow: requires native compilation (installed via pkg)
# - cryptography: Rust-based library (installed via pkg)
# See install.sh for proper installation steps

# Core Dependencies (httpx is used by google-generativeai)
httpx>=0.27.0

# Terminal UI
rich>=13.7.0
prompt-toolkit>=3.0.43

# Configuration
toml>=0.10.2

# File Processing (Pillow installed via pkg)
PyPDF2>=3.0.1

# Utilities
python-dateutil>=2.9.0
aiofiles>=23.2.1
